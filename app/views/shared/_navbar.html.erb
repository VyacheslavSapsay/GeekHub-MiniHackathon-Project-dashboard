<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "DashBoard", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if current_user %>
        <li class="nav-item active">
          <%= link_to current_user&.email, user_path(current_user), class: "nav-link" %>
        </li>
        <li class="nav-item active">
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
      <% else %>
        <%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path %>
      <% end %>
      <li class="nav-item">
        <%= link_to "Create Post", new_post_path, class: "nav-link btn btn-outline-dark" if current_user %>
      </li>
      <li class="nav-item">
        <%= link_to "Create Category", new_category_path, class: "nav-link" if current_user&.admin? if current_user %>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <%= text_field_tag :search, params[:search], placeholder: "Search Posts", class: "form-control" %>
      <%= submit_tag "Search", class: "btn btn-outline-primary" %>
    </form>
  </div>
</nav>
